[project]
name = "inverse-eval"
version = "0.1.0"
description = "Evaluation pipeline for reflectometry parameter estimation experiments."
readme = "README.md"
requires-python = ">=3.10"

[tool.uv]
package = false
